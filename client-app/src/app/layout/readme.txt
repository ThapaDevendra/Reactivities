//using React "Fragment" instead of "div" because "div" in tag do not provides any styling
classes or doing anything but appears in html. Therefore we use "Fragment" or leave
empty tag like "<>" because multiple components are required to place inside "Fragment".
//inverted is darker color, add to the top of the page defined by fix
//container helps padding
// if there was no image alt attributes displays its content 